# ROMTool – Python dependencies
# Install with:  pip install -r requirements.txt

PySide6>=6.6.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.1.0          # faster BS4 parser (optional but recommended)
py7zr>=0.21.0        # 7z extraction (pure Python)
rarfile>=4.1         # RAR extraction (requires unrar on PATH)
pyinstaller>=6.5.0   # packaging only
